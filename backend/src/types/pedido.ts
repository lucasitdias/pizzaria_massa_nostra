/*
import { PizzaPersonalizada } from './pizza'

export interface ItemPedido {
  tipo: 'pizza' | 'bebida'
  pizzaId?: number
  bebidaId?: number
  quantidade: number
  tipoPizza?: PizzaPersonalizada['tipo']
  tamanho?: PizzaPersonalizada['tamanho']
  sabores?: PizzaPersonalizada['sabores']
  borda?: PizzaPersonalizada['borda']
  recheioBorda?: PizzaPersonalizada['recheioBorda']
  insumos?: {
    insumoId: number
    quantidadeUtilizada: number
  }[]
}

export interface Pedido {
  clienteId: number
  centroDeCustoId: number
  formaPagamento: string
  observacoes?: string
  itens: ItemPedido[]
}

*/